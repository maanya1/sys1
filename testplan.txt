Different file inputs
- empty file
  - Build Codebook
  - Compress
  - Decompress

-file with 1 word
  - Build Codebook
  - Compress
  - Decompress

-file with multiple words repeating multiple times
-file with 1 word repeating multiple times and every other word
- recursive etc.

Later:
- Overwriting files

Different inputs to the terminal
-the input should be ./fileCompressor <flag> <path or file> |codebook|
-inputting too many flags
-inputting not enough flags
-inputting flags in the wrong order 
-inputting flags that do not exist (anything other than -b, -c, -d, or -R)
-no mixing flags, (ex. must be -R -b, not -Rb)
-giving an invalid pathname or filename
